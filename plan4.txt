I will define:
```go
var ErrUsernameRequired = errors.New("username is required for privilege drop")
```
Where should it go?
If I put it in `drop_privileges_windows.go`, then it will only exist on Windows build, and Unix build won't have it (or Unix build would return a dynamically created error). Wait, the PR comment is specifically on `drop_privileges_windows.go` line 12:
```go
	if username == "" {
-		return fmt.Errorf("username is required for privilege drop")
+		return errors.New("username is required for privilege drop")
```
The user said: "@jules can we promote these to globlas so we can capture them too?"
"these" implies both errors in this file!
```go
var ErrUsernameRequired = errors.New("username is required for privilege drop")
var ErrDropPrivilegesNotSupported = fmt.Errorf("privilege drop is not supported on windows: %w", errors.ErrUnsupported)
```
Wait, you cannot have `fmt.Errorf` in a global initialization if it relies on `%w`, well, actually you can, it just wraps it once at init time, which is fine since `errors.ErrUnsupported` is immutable.

Wait, if I define `ErrUsernameRequired` in `drop_privileges_windows.go`, it won't be available on non-Windows builds. If a user tries to capture it, they might fail to compile on Unix if they reference `cli.ErrUsernameRequired` and it's not defined there.
BUT they might also use `cli.ErrDropPrivilegesNotSupported` on Unix? Probably not, they'd just write Windows specific tests.

Let's check `cli/drop_privileges_windows.go`:
```go
//go:build windows

package cli

import (
	"errors"
	"fmt"
)

var (
	ErrUsernameRequired = errors.New("username is required for privilege drop")
	ErrDropPrivilegesNotSupported = fmt.Errorf("privilege drop is not supported on windows: %w", errors.ErrUnsupported)
)

func DropPrivileges(username string) error {
	if username == "" {
		return ErrUsernameRequired
	}
	return ErrDropPrivilegesNotSupported
}
```
If I update `drop_privileges_unix.go` as well to use `ErrUsernameRequired` and maybe other globals, it would be more consistent!
```go
// in drop_privileges_unix.go
var (
	ErrUsernameRequired = errors.New("username is required for privilege drop")
	ErrRequiresRoot     = errors.New("-login requires root privileges to change credentials")
)
```
Wait, if I declare `ErrUsernameRequired` in BOTH files, the compiler will complain about "ErrUsernameRequired redeclared in this block" when building.
Wait, they are mutually exclusive because of `//go:build windows` and `//go:build !windows`. So it's fine!

Let me make these changes and see what `go build` does.
