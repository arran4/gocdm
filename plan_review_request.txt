The user wants me to promote the errors to globals so they can capture them too.
This means I should create exported (or unexported?) error variables for:
1. "username is required for privilege drop" -> e.g. `ErrUsernameRequired`
2. "privilege drop is not supported on windows: %w" -> Wait, for the second one, they might just want the first one to be a global, or both?
Looking at the code:
```go
var ErrUsernameRequired = errors.New("username is required for privilege drop")
```
Wait, the comment is on line 12:
```go
	if username == "" {
		return errors.New("username is required for privilege drop")
```
If we promote these to globals (i.e., `ErrUsernameRequired`), it makes sense to define it either in `cli.go` or in `drop_privileges_unix.go`/`drop_privileges_windows.go`.
Let's see if `drop_privileges_unix.go` has similar errors.
